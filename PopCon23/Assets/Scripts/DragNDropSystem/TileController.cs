using System.Collections;
using System.Collections.Generic;
using UnityEngine;


public class TileController : MonoBehaviour
{

    //this variable is used to determine if the tile is currently empty or not. Since some tiles will be filled by default, I am setting this variable as public so that we can change it if need be
    public bool isEmpty = true;

}